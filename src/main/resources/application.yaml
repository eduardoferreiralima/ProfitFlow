server:
  port: 8888
  error:
    include-message: always
    include-binding-errors: always

spring:
  application:
    name: ProfitFlow

  datasource:
    driver-class-name: ${SPRING_DATASOURCE_DRIVER:org.h2.Driver}
    url: ${SPRING_DATASOURCE_URL:jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL}
    username: ${SPRING_DATASOURCE_USERNAME:sa}
    password: ${SPRING_DATASOURCE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

  flyway:
    baseline-on-migrate: true

springdoc:
  swagger-ui:
    path: /api/docs
    url: /v3/api-docs
    server-url: http://localhost:8888

cors:
  allowed-origins: "*"
  allowed-methods: "GET,POST,PATCH,PUT,DELETE,OPTIONS"

flyway:
  enabled: true
  locations: classpath:db/migration

api:
  security:
    token:
      secret: ${SPRING_SECURITY_SECRET}
      issuer: ${SPRING_SECURITY_ISSUER}
      expire-access-token: ${SPRING_SECURITY_EXPIRE_ACCESS_TOKEN}
      expire-refresh-token: ${SPRING_SECURITY_EXPIRE_REFRESH_TOKEN}